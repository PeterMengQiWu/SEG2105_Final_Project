<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uottawa.leyaoli.seg2105_final_project">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/vacuum_cleaner"
        android:label="@string/app_name"
        android:roundIcon="@drawable/vacuum_cleaner"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddChore" />
        <activity android:name=".ChoreDetail" />
        <activity android:name=".PersonalFile" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".StartPage" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".AccountSetting" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".CupBoard" />
        <activity android:name=".Tools" />
        <activity android:name=".ChangeName" />
        <activity android:name=".ChooseWorkerActivity" />
        <activity android:name=".TaskBaclogActivity"></activity>
    </application>

</manifest>